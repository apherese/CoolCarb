<%= content_for(:navbar_class, "navbar-home") %>

<% if current_user.nil? %>
  <% url_path = new_company_url %>
<% else %>
  <% url_path = dashboard_url %>
<% end %>

<div class="banner">
  <div class="banner-content text-center">
    <h1>La plate-forme des PME engagées pour le climat!</h1>
    <p>Décarbonnez votre empreinte</p>
    <%= link_to "Calculez facilement votre bilan carbone", url_path, class: "btn btn-lg btn-primary" %>
  </div>
</div>
